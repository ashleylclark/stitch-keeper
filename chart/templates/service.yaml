apiVersion: v1
kind: Service
metadata:
  name: {{ include "stitch-keeper.fullname" . }}
  labels:
    {{- include "stitch-keeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stitch-keeper.selectorLabels" . | nindent 4 }}
